package Technology.Game;

import KeyboardPang.TextureBank;
import android.content.Context;
import android.graphics.Bitmap;

public class ObjectFactory {
	/*static ObjectFactory factory = null;
	
	public static ObjectFactory getInstance(){
		if(factory == null){
			factory = new ObjectFactory();
		}
		return factory;
	}
	
	private ObjectFactory(){ nothing }
	
	
	 * CreateObject Method Design by Contract
	 * @pre : �ݵ�� ���� ������ Ŭ����
	 * @post : ���� null ���� ��ȯ�Ǽ��� �ȵȴ�.
	 * 
	public GameObject CreateObject(String name){
		GameObject obj = null;
		
		assert(obj == null);
		return obj;
	}
	
	public GameObject CreateObject(String name,String id,float x,float y,Bitmap bitmap){
		GameObject obj = null;
		
		if(name.equals("GameObject")){
			obj = new GameObject(id,x,y);
			obj.SetBitmap(bitmap);
		}else if(name.equals("GamePlayer")){
			obj = new GamePlayer(id, x, y);
		}
		
		assert(obj == null);
		return obj;
	}
	public GameObject CreateObject(String name,String id,float x,float y,Context context,int bitmapId){
		GameObject obj = null;
		
		if(name.equals("GameObject")){
			obj = new GameObject(id,x,y);
			//Bitmap bitmap = TextureBank.LoadHouseBitmap();
			obj.SetBitmap(bitmap);
		}else if(name.equals("GamePlayer")){
			obj = new GamePlayer(id,x,y);
			//Bitmap bitmap = TextureBank.LoadHouseBitmap();
			obj.SetBitmap(bitmap);
		}
		
		assert(obj == null);
		return obj;
	}*/
}

