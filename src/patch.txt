1. GameWorld에 Remove를 Safe하게 패치. (제거시에 오류가 났었다.)
2. 오류해결을 했다. 역시 쓰레드의 동기화 문제였다. GamwView에서 mHolder에 동기화를 걸어주었다. 또한 게임월드에 안전 삭제도 추가했다.
3. GameButton 오브젝트를 추가하였다.
4. 애니메이션 기능은 sprite 기능으로 대채되었다. 이미지 투명화도 포토샵에서 지정해주고 그대로 로드하면 문제가 없다.
5. 애니메이션 자동화 완성했다.
6. 애니메이션을 하면서 움직이고 크기도 줄어들수도 있고, 회전도 할수 있는 GameParticleObject가 추가되었다. 아직 크기와 함꼐 사용하면 불안정하다.
7. ImageGameButton을 만들었고, GameButton과 ImageGameButton에 사용자 정의 ActionTouch 컨트롤을 설정할수 있게하였다.
8. Sound기능을 하는 GameSound 클래스를 추가하였다. 이것은 데이터를 로드하여 Play로 사운드를 플레이할수 있으며 기본설정 최대 출력은 32개로 설정되었다. 이로써 게임에 사운드를 추가할수 있게되었다.
9. Sprite의 확장버전인 SpriteEx를 만들었따.121023
10. Animation프로그래스바를 만들었따. 121023

11. 터치를 50번이상 동시에 실행하면 렉이걸리면서 꺼져버린다. 아마도 터치이벤트 동기화 문제일것같다. 펀치 이펙트에 문제가있었다. 하지만 이번기회로 쓰레드의 동기화와 터치이벤트간의 동기화를 해결했다. 또한 이것으로 인해 World의 비정상적 동기화호출을 막을수 있었다.
12. 하지만 렉이 여전히 있다.... 느려지게 되면 팅긴다. 아마도... 터치문제가 확실하다. 이걸 언제고치지.. 121023

13. 에러의 원인을 찾았다. 에러의 원인은 바로 Sprite 객체의 알파비트맵에서 Recycilig을 안해서 발생하는 OutOfMemory예외 였다.
14. 뒤고라기 버튼을 누르면 꺼지게 만들었다.

121026
15. 광고를 할수있도록 XML에 이 뷰를 넣을수 있도록하였다. 하지만 .. 광고가 걸려 인터럽트가 발생하면 쓰레드가 강제 종료된다. 이를 해결해야한다.
16. 한가지 뻘짓으로 canvas.DrawRect 함수 호출할때 paint 인자값을 null로 줘버려서 계쏙 뻑이 났었다. 지금은 해결했다.
17. 쓰레드를 안전하게 정지하고 프로그램을 정지하니까 원래 생기던 강제로 중지된것 같은 메세지 박스가 사라졌다. 쓰레드를 안전하게 정지, 시작, 일시정지를 개발해야한다. 
18. 쓰레드를 안전하게 정지 , 일시정지, 재시작을 구현했다. 이 구현물은  Gameview 에 구현되어있다. 
19. 어느정도 광고 작업은 되었다. 하지만 몇몇광고가 뜨면 액티비티가 멈추는 경우가 있따. 이거를 해결해야겠다.
20. 클래스안에서 쓰레드를 생성하지 않도록 구조를 조정하였다. 그 이유는 핸드폰에서 바탕화면으로 나갈때 쓰레드를 죽이고 나가기 때문이다. 따라서 다시 바탕화면으로 돌아올때 우리는 쓰레드를 다시 실행하여 프로그램을
실행할것이다.

21. Technology 엔진의 구조를 많이 개선했다. 이제 포어그라운드, 백그라운드 교체간에 오류가 날일은 없을것이다.
또한 광고부착도 모두 완성했다. 새로생긴 클래스로는 TechnologyActivity 가 있고 이 액티비티를 상속받아서 메인액티비티를 만든다.

22. 비트맵 로더를 만들었다. 이 클래스는 비트맵을 로드하고 불러오는 역할을 한다.
23.터치하면 이상하게 되는 문제를 해결해야한다.

24. GameWorld에 치명적인 오류를 개선했다. 무슨이유인지 모르겠지만 ArrayList의 remove(object) 가 문제였다. 삭제가 맨 뒤에서부터 삭제되는 문재ㅔ가 있었기에 이를 수정하였다. 
//제거 오류 수정
25. Timer 추가
26. GameWorld의 Add 함수를 바꾸었다. 기존에는 Update ,혹은 Draw에서 오브젝트의 추가는 안됬지만 이제는 허용된다. 3중 추가 , 삭제로 인해서..
27. 월드에서 Add하는게 아직 뭔가 불안하다. 이것을 수정해야한다. GAmeWorld의 추가 문제인것같다.

*6. 엔진코어계층과 엔진응용계층사이의 벽을 만들었다. 이 벽은 엔진응용계층에는 final이 붙어서 상속을 못받게했다.(KeyboardPang 패키지안에 있는 것들).
그 이유는 이 엔진응용계층에서 월드로 직접적으로 추가와 삭제를 하기 때문이다. 이것은 OOP5대 원칙을 원천적으로 무너뜨림으로써 얻는 사용자의 편리함이다. 하지만 그만큼 구조가 불안하고 변경되기 쉬우므로 절때로 상속을 못받도록

final 명령어로 상속을 금지시켰다. 상속은 반드시 엔진코어계층으로써만 받도록 한다.


[텍스쳐 등록방법]
TextureBank에 가서 static으로 변수를 만들고 텍스쳐를 불러온다.





http://www.freesfx.co.uk/download/?type=mp3&id=6311 에서 사운드를 받아왔다. 이 사이트는 무료로 이펙트 사운드를 주는대신에 자신의 홈페이지를 프로젝트에 개제해달라는 요청이 있다. (http://www.freesfx.co.uk) 이거